// import Index from '~/pages/index.vue';
// import { mount } from '@vue/test-utils';

describe('MainPage', () => {
    it('is a Component like Index?', () => {
        // const wrapper = mount(Index);
        // expect(wrapper.isVueInstance()).toBeFalsy();
    });
});